<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <groupId>org.springframework.integration</groupId>
    <artifactId>org.motechproject.org.springframework.integration.spring-integration-core</artifactId>
    <version>${org.springframework.integration.version}-${release.tag}</version>
    <packaging>bundle</packaging>
    <name>Spring Integration Core</name>
    <description>
        Spring Integration is motivated by the following goals:

        Provide a simple model for implementing complex enterprise integration solutions.
        Facilitate asynchronous, message-driven behavior within a Spring-based application.
        Promote intuitive, incremental adoption for existing Spring users
    </description>


    <parent>
        <artifactId>external-osgi-bundles</artifactId>
        <groupId>org.motechproject</groupId>
        <version>1.0.14</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <properties>
        <modules.root.dir>${basedir}/..</modules.root.dir>
        <org.springframework.integration.version>4.3.12.RELEASE</org.springframework.integration.version>
    </properties>


    <dependencies>
        <dependency>
            <groupId>org.springframework.integration</groupId>
            <artifactId>spring-integration-core</artifactId>
            <version>${org.springframework.integration.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>com.fasterxml.jackson.core</groupId>
                    <artifactId>jackson-databind</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>

    <build>
        <plugins>

            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack-sources</id>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <phase>package</phase>
                        <configuration>
                            <outputDirectory>${project.build.directory}/sources</outputDirectory>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.springframework.integration</groupId>
                                    <artifactId>spring-integration-core</artifactId>
                                    <version>${org.springframework.integration.version}</version>
                                    <classifier>sources</classifier>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <version>3.5.1</version>
                <extensions>true</extensions>
                <configuration>
                    <instructions>
                        <Import-Package>
                            com.esotericsoftware.kryo;resolution:=optional,
                            com.esotericsoftware.kryo.io;resolution:=optional,
                            com.esotericsoftware.kryo.pool;resolution:=optional,
                            com.jayway.jsonpath;resolution:=optional,
                            javax.xml.parsers,
                            com.fasterxml.*;resolution:=optional,
                            org.aopalliance.aop;resolution:=optional,
                            org.aopalliance.intercept;resolution:=optional,
                            org.apache.commons.logging;resolution:=optional,
                            org.boon.json;resolution:=optional,
                            org.objenesis.strategy;resolution:=optional,
                            org.springframework.*,
                            org.w3c.dom,
                            reactor*;resolution:=optional
                        </Import-Package>
                        <Export-Package>
                            org.springframework.integration.*;version=${org.springframework.integration.version}
                        </Export-Package>
                        <Bundle-Vendor>Spring</Bundle-Vendor>
                        <Bundle-SymbolicName>${project.artifactId}</Bundle-SymbolicName>
                        <Embed-Dependency>spring-integration-core;inline=true</Embed-Dependency>
                    </instructions>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>